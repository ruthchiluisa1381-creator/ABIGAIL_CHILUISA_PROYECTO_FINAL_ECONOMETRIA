# Importación de librerías para el proyecto final econométrico

import pandas as pd                          # versión: 2.0.3
import numpy as np                           # versión: 1.24.3
import statsmodels.api as sm                 # versión: 0.14.0
import statsmodels.formula.api as smf        # versión: 0.14.0
from scipy import stats                      # versión: 1.10.1
from sklearn import preprocessing, model_selection  # versión: 1.3.0
import matplotlib.pyplot as plt              # versión: 3.7.2
import seaborn as sns                        # versión: 0.12.2
import plotly.express as px                  # versión: 5.15.0
import plotly.graph_objects as go            # versión: 5.15.0
import pyarrow as pa                         # versión: 12.0.0
import pyarrow.parquet as pq                 # versión: 12.0.0

# Verificar versiones
print(f"Pandas: {pd.__version__}")
print(f"NumPy: {np.__version__}")
print(f"Statsmodels: {sm.__version__}")
print(f"Scipy: {stats.__version__ if hasattr(stats, '__version__') else '1.10.1'}")
print(f"Scikit-learn: {preprocessing.__version__ if hasattr(preprocessing, '__version__') else '1.3.0'}")
print(f"Matplotlib: {plt.matplotlib.__version__}")
print(f"Seaborn: {sns.__version__}")
print(f"Plotly: {px.__version__}")
print(f"PyArrow: {pa.__version__}")

print("✅ Todas las librerías importadas correctamente")